# @tramio/clients

REST wrappers for the self-hosted backend: `Catalog_Client` and
`Entitlement_Client`. Also owns the in-tour HTTP chokepoint that enforces
"no cellular calls during an active tour" (Property 15).

Module boundary set up in task 1.3. Implementation tracked under tasks 6.2–6.6.
