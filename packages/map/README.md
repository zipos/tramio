# @tramio/map

MapLibre GL Native integration. Serves vector tiles offline from
`${docs}/packs/{bundleId}/{version}/tiles/` (no outbound tile requests during
an active tour) and provides the persistent OpenStreetMap attribution overlay.

Module boundary set up in task 1.3. Implementation tracked under tasks 10.1–10.3.
