{
  "name": "@tramio/backend",
  "version": "0.0.0",
  "private": true,
  "description": "Self-hosted Fastify backend: Catalog_Service, Entitlement_Service, Moderation Store, signed responses.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "serve": "ts-node src/bin/serve.ts"
  },
  "dependencies": {
    "fastify": "^5.0.0",
    "fastify-plugin": "^5.0.0"
  }
}
