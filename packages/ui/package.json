{
  "name": "@tramio/ui",
  "version": "0.0.0",
  "private": true,
  "description": "React Native UI: route selection, tour playback, deviation prompt, storage management, attribution, sponsored disclosure.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest"
  }
}
